# 2026-04-28 工作日志

## 数据库连接测试
- ✅ MySQL (TiDB Cloud) 连接正常，14 张表可访问
- ❌ MongoDB Atlas 连接失败 — DNS 解析到 198.18.6.238（保留地址），TLS 握手无法建立，疑为网络环境/防火墙限制。项目业务代码不依赖 MongoDB。

## 地图看板初始定位优化
- **问题**：地图看板每次打开默认显示天安门，等 GPS 定位完成（几秒）后才跳到用户位置
- **根因**：AMapWebView 初始化时 `initLng/initLat` 为 null（bloc 还没拿到 GPS），高德地图 JS API 默认中心点为北京
- **修复**（map_dashboard_screen.dart）：
  1. `initState` 中调用 `Geolocator.getLastKnownPosition()` 获取设备缓存的最后已知位置（瞬间返回）
  2. WGS84→GCJ02 坐标转换后存入 `_cachedLat/_cachedLng`
  3. `build` 方法中 `initLng/initLat` 回退到缓存位置
  4. `_syncAll` 中 GPS 获取成功后更新缓存位置，bloc 还没拿到 GPS 时也用缓存位置移动地图

## 水印拍照定位失败 + 周边地址列表修复
- **问题1**：水印拍照界面定位一直失败，显示"定位中..."
- **问题2**：点击地址字段后无法打开周边地址列表，或者列表为空
- **根因**：
  1. `_initGeo()` 使用 `forceAndroidLocationManager: true` 强制 Android 原生 LocationManager，在某些设备上定位失败
  2. `_loadNearbyPlaces()` 加载了高德周边搜索结果，但结果没有保存到状态变量中，导致列表为空
  3. `_openLocationPicker()` 在 `_latitude/_longitude` 为 null 时直接 return，用户点击无响应
- **修复**（camera_checkin_screen.dart）：
  1. 移除 `forceAndroidLocationManager: true`，改用 FusedLocationProvider（更可靠）
  2. 增加超时到 15 秒，失败时先用 `getLastKnownPosition()` 回退
  3. 新增 `_retryLowAccuracyGeo()` 低精度回退方法
  4. 从 AttendanceBloc 回退后，如果 bloc 有位置也执行逆地理编码和周边搜索
  5. 新增 `_nearbyPlaces` 状态变量，`_loadNearbyPlaces()` 把结果保存到其中
  6. `_openLocationPicker()` 没有坐标时先尝试重新定位，而不是直接 return
  7. `_LocationPickerScreen` 新增 `preloadedPlaces` 参数，传入已加载的地址列表，一打开就能直接显示

## 水印字段编辑不生效修复
- **问题**：编辑水印字段（如打卡名称）后，相机上的水印文字没有更新，保持模板默认值
- **根因**：`_getSlotValue` 中 `customText`（模板硬编码文字）优先级高于用户数据，导致编辑无效
- **修复**（camera_checkin_screen.dart）：
  1. `_getSlotValue` 改为数据优先：先从 data Map 读取用户编辑后的值，只有为空时才回退到模板默认文字
  2. `_editTextfield` 改为 `Future<void>` 异步方法，用 `showDialog<String>()` 返回结果
  3. `_editTemplateField` 正确 `await` 编辑对话框

## 水印字段关联混乱修复
- **问题**：修改面板上的"打卡名称"会同时影响"施工区域""施工内容"等其他字段，显示都变成同一个值
- **根因**：`_customCheckinName` 被多处复用为默认值（`_selectedProjectName` 回退、`_defaultEditableValue` 中 workContent 等字段回退、`_buildWatermarkData` 中 workContent 回退）
- **修复**（camera_checkin_screen.dart）：
  1. 新增 `_watermarkTitle` 独立变量，专门用于水印主标题，不影响其他字段
  2. `_selectedProjectName` 不再回退到 `_customCheckinName`
  3. `_defaultEditableValue` 中 workContent 等字段默认值改为 `''`
  4. `_buildWatermarkData` 中 `'workContent'` 不再回退到 `_customCheckinName`
  5. 面板"打卡名称"编辑 `_watermarkTitle`，同时同步更新模板 `titleSlot.customText`
  6. 新增 `_syncTitleToTemplate()` 方法，切换打卡类型/选择项目时同步更新水印主标题
  7. `_getFieldValueFromData` 中 projectName 空值回退改为 `'未关联项目'`，workContent 空值回退改为 `'点击编辑本条内容'`

## 水印主标题被项目名称覆盖修复
- **问题**：打卡名称设为"项目进度上报"，但水印主标题仍显示"一层地面"
- **根因**：`_syncTitleToTemplate()` 只改了 `titleSlot.customText`，但 `binding='project'` 仍在，`_getSlotValue` 优先级 `binding > customText`
- **修复**：
  1. `_syncTitleToTemplate()` 清空 `binding`，同步 `label` 和 `customText` 为 `_watermarkTitle`
  2. 编辑打卡名称时统一调用 `_syncTitleToTemplate()`
  3. `_WatermarkPreviewBuilder._getSlotValue()` 统一取值顺序为 `binding → customText → label`，与 `WatermarkService` 一致

## 水印设置面板字段与水印预览不一致修复
- **问题**：设置面板显示"施工内容"，水印显示"施工进度"；面板有"施工区域"但水印没有；面板显示"地点"水印显示"位置"
- **根因**：设置面板遍历 `template.fields`（提取的 `WatermarkFieldType`），label 用硬编码 `_fieldTitle()`；水印预览遍历 `template.contentSlots`，label 用 `slot.label`。来源不同导致不一致
- **修复**（camera_checkin_screen.dart）：
  1. 新增 `_getSlotDisplayValue(WatermarkSlot)`，与水印预览取值逻辑完全一致
  2. 重构 `_showWatermarkEditPanel`："当前模板字段"部分改为遍历 `subtitleSlot` + `contentSlots`
  3. 每个字段的 label 直接使用 `slot.label`，value 使用 `_getSlotDisplayValue(slot)`
  4. 新增 `_buildWatermarkSettingRowForSlot()` 辅助方法
  5. 兜底逻辑 `_buildDefaultWatermarkSettingRows()` 仅在模板未加载时使用

## P0 技术债修复（2026-04-28 晚）
### 1. 环境变量化敏感配置
- **新增** `BackEnd/server/.env` — 存放所有敏感配置（数据库密码、JWT 密钥、高德 Key）
- **新增** `BackEnd/server/.env.example` — 环境变量模板
- **修改** `config/index.js` — 移除所有硬编码的默认值（密码、密钥、Key），改为从 `process.env` 读取
- **修改** `app.js` — 顶部引入 `dotenv/config`，启动时自动加载 `.env`
- **安装** `dotenv` 依赖

### 2. 统一日志框架（winston）
- **新增** `utils/logger.js` — 基于 winston 的日志工具，支持：
  - 日志级别（error/warn/info/http/verbose/debug/silly）
  - 文件轮转（combined.log / error.log / exceptions.log / rejections.log）
  - 结构化输出（JSON metadata）
  - 开发环境彩色控制台输出
- **修改** `app.js` — Socket.IO 事件、启动流程、健康检查改用 `logger`
- **修改** `models/db.js` — 所有 `console.log`/`console.error` 改为 `logger.info`/`logger.error`/`logger.warn`
- **修改** `controllers/dashboardController.js` — 所有错误处理改用 `logger.error`
- **修改** `controllers/locationController.js` — 所有错误处理改用 `logger.error`，SQL 注入修复
- **安装** `winston` 依赖

### 3. 修复空 catch 块
- **BackEnd/web-admin/src/views/Users.vue** — 5 个空 catch 改为 `ElMessage.error()` 提示用户
- **BackEnd/web-admin/src/views/Checkin.vue** — 2 个空 catch 改为错误提示
- **BackEnd/web-admin/src/views/Projects.vue** — 3 个空 catch 改为错误提示
- **BackEnd/server/controllers/dashboardController.js** — 2 个降级查询的空 catch 改为 `logger.warn`
- **MobileApp** — 18 个空 catch 全部补全：
  - `auth_bloc.dart` ×2 → `debugPrint`
  - `home_bloc.dart` ×1 → `debugPrint`
  - `attendance_bloc.dart` ×1 → `debugPrint`
  - `camera_checkin_screen.dart` ×5 → `debugPrint`
  - `map_dashboard_screen.dart` ×1 → `debugPrint`
  - `home_screen.dart` ×2 → `debugPrint`
  - `profile_screen.dart` ×1 → `debugPrint`
  - `chat_screen.dart` ×1 → `debugPrint`
  - `chat_quote_codec.dart` ×1 → `debugPrint`
  - `notification_repository.dart` ×1 → `debugPrint`
  - `watermark_service.dart` ×1 → `debugPrint`
  - `local_notification_service.dart` ×1 → `debugPrint`

### 4. 修复 SQL 注入风险
- **locationController.js** `getLocationHistory()` — `LIMIT ${pageSize} OFFSET ${offset}` 改为参数化查询 `LIMIT ? OFFSET ?`
- **dashboardController.js** `getAnomalies()` — 已确认 `limit` 经过 `Math.min(parseInt(...), 20)` 校验，但已改为 `db.execute` + 参数化（如后续需要）
