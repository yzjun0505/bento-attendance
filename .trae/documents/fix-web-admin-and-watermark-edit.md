# 修复 Web 管理端问题 + 水印编辑功能

## 问题分析

### 1. 项目管理和设备管理打不开

经检查：

* 后端控制器文件存在且完整 (`projectController.js`, `deviceController.js`)

* 后端路由配置正确

* 前端 API 调用路径正确

可能原因：

* 后端服务未运行

* 数据库表不存在

**解决方案**：

1. 确保后端服务正在运行
2. 检查数据库是否有 `projects` 和 `devices` 表

### 2. 水印打卡允许编辑水印

需求：在拍照后允许用户编辑水印信息

## 修复计划

### 第一部分：确保后端服务正常

1. 检查数据库表是否存在
2. 重启后端服务

### 第二部分：水印编辑功能

修改 `camera_checkin_screen.dart`：

1. 拍照后显示编辑界面，允许用户：

   * 选择/修改关联项目

   * 添加备注信息

   * <br />

2. 界面布局：

   * 拍照后显示照片预览

   * 可以修改水印位置和大小

   * 底部显示编辑表单（项目选择、备注输入）

   * 确认后提交打卡

## 文件修改清单

1. `lib/screens/attendance/camera_checkin_screen.dart` - 添加水印编辑功能

