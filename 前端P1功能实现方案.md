# P1 功能模块前端实现方案

> 基于已完成后端 API（排班/班次、节假日、审批联动、离线打卡、轨迹回放），给出 Web 管理端（Vue3）和 Flutter 移动端的具体改动方案。

---

## 项目结构速览

### Web 管理端（`BackEnd/web-admin/`）
- **框架**: Vue 3 + Vite + Element Plus
- **状态管理**: Pinia
- **HTTP**: Axios（封装在 `src/api/request.js`，自动注入 token）
- **路由**: Vue Router（`src/router/index.js`）
- **布局**: `Layout.vue`（悬浮顶栏 + 高频/低频导航）

### Flutter 移动端（`MobileApp/`）
- **框架**: Flutter 3.x + BLoC（flutter_bloc）
- **HTTP**: Dio（`api/dio_client.dart`，自动 token + 刷新）
- **导航**: 底部 4 栏（`main_navigation.dart`：消息/打卡/明细/我的）
- **UI**: 自定义 Bento Design（`bento_colors.dart`）
- **定位**: geolocator + 高德地图 WebView

---

## 一、排班 / 班次管理

### 1.1 Web 管理端

#### 新增页面

| 页面 | 路由 | 文件 |
|------|------|------|
| 班次管理 | `/shifts` | `views/Shifts.vue` |
| 排班日历 | `/schedules` | `views/Schedules.vue` |

#### 需要修改的文件

**`src/router/index.js`** — 注册新路由：

```js
{
  path: 'shifts',
  name: 'Shifts',
  component: () => import('@/views/Shifts.vue'),
  meta: { title: '班次管理', icon: 'Clock' }
},
{
  path: 'schedules',
  name: 'Schedules',
  component: () => import('@/views/Schedules.vue'),
  meta: { title: '排班管理', icon: 'Calendar' }
}
```

**`src/api/shifts.js`** — 新增 API 封装：

```js
import request from './request'

export const getShifts = (params) => request.get('/shifts', { params })
export const getShiftById = (id) => request.get(`/shifts/${id}`)
export const createShift = (data) => request.post('/shifts', data)
export const updateShift = (id, data) => request.put(`/shifts/${id}`, data)
export const deleteShift = (id) => request.delete(`/shifts/${id}`)
```

**`src/api/schedules.js`** — 新增 API 封装：

```js
import request from './request'

export const getSchedules = (params) => request.get('/schedules', { params })
export const batchSchedule = (data) => request.post('/schedules/batch', data)
export const setRestDay = (id, isRest) => request.put(`/schedules/${id}/rest`, { is_rest: isRest })
export const deleteSchedule = (id) => request.delete(`/schedules/${id}`)
export const getCalendar = (date) => request.get('/schedules/calendar', { params: { date } })
```

**`src/components/Layout.vue`** — 把新页面加入导航菜单：
- `highFreqMenus` 或 `lowFreqMenus` 中加入 `{ path: '/shifts', title: '班次管理', icon: 'Clock' }`
- `{ path: '/schedules', title: '排班管理', icon: 'Calendar' }`

#### `Shifts.vue` 页面设计

- **顶部**: 搜索框（按名称筛选）+ "新增班次" 按钮
- **表格**: Element Plus `el-table`，列：名称、上班时间、下班时间、迟到容忍、早退容忍、颜色标识、状态、操作（编辑/删除）
- **弹窗**: `el-dialog` 表单 — 名称、时间选择器（`el-time-picker`）、数字输入框（容忍分钟）、颜色选择器（`el-color-picker`）
- **颜色标识**: 用 `el-tag` 或色块显示班次颜色

#### `Schedules.vue` 页面设计

- **左侧**: 人员选择器（`el-select` 多选，从 `/users` 拉取）
- **中间**: 日历组件（可用 `el-calendar` 或第三方 `vue-cal`）
- **右侧**: 班次选择器（从 `/shifts` 拉取列表）
- **批量排班**: 选择日期范围 → 选择班次 → 勾选"周末休息" → 点击"批量排班"
- **日历格子**: 显示当天班次名称（带颜色）或"休"字标识
- **点击日期**: 弹出当天排班详情，可修改/删除

---

### 1.2 Flutter 移动端

#### 需要修改的文件

**`lib/repositories/schedule_repository.dart`** — 新增 Repository：

```dart
class ScheduleRepository {
  final ApiClient apiClient;
  ScheduleRepository({required this.apiClient});

  Future<Map<String, dynamic>?> getTodaySchedule() async {
    final resp = await apiClient.dio.get('/schedules/today');
    if (resp.statusCode == 200 && resp.data['code'] == 200) {
      return resp.data['data'];
    }
    return null;
  }
}
```

**`lib/blocs/attendance/attendance_bloc.dart`** — 加载数据时获取今日排班：

在 `_onLoadAttendanceData` 的 `Future.wait` 中增加：

```dart
ScheduleRepository(apiClient: _apiClient).getTodaySchedule().catchError((e) => null),
```

然后在 `AttendanceLoaded` 中新增字段：`todayShiftName`、`todayShiftStart`、`todayShiftEnd`。

**`lib/screens/attendance/map_dashboard_screen.dart`** — 在打卡面板显示今日班次：

在打卡按钮上方增加一个卡片：
- 标题："今日排班：白班"
- 内容："08:00 - 17:00 · 迟到容忍 15 分钟"
- 颜色：用班次的 `color` 字段

**`lib/screens/calendar/attendance_calendar_screen.dart`** — 增强日历视图：

- 从 `/schedules?date_start=...&date_end=...` 拉取排班数据
- 在日历格子里显示班次名称和颜色
- 休息日显示灰色"休"

---

## 二、节假日 / 工作日设置

### 2.1 Web 管理端

#### 新增页面

| 页面 | 路由 | 文件 |
|------|------|------|
| 节假日管理 | `/holidays` | `views/Holidays.vue` |

#### 需要修改的文件

**`src/router/index.js`** — 注册路由：

```js
{
  path: 'holidays',
  name: 'Holidays',
  component: () => import('@/views/Holidays.vue'),
  meta: { title: '节假日管理', icon: 'Collection' }
}
```

**`src/api/holidays.js`** — 新增 API 封装：

```js
import request from './request'

export const getHolidays = (year) => request.get('/holidays', { params: { year } })
export const createHoliday = (data) => request.post('/holidays', data)
export const batchCreateHolidays = (data) => request.post('/holidays/batch', data)
export const updateHoliday = (id, data) => request.put(`/holidays/${id}`, data)
export const deleteHoliday = (id) => request.delete(`/holidays/${id}`)
export const checkDate = (date) => request.get('/holidays/check', { params: { date } })
```

**`src/components/Layout.vue`** — 加入低频菜单（"基础设置"下拉）

#### `Holidays.vue` 页面设计

- **顶部**: 年份选择器（`el-date-picker` type="year"）+ "新增节假日" + "批量导入"
- **表格**: 列：日期、名称、类型（放假/调休上班）、操作
- **类型标签**: 放假 → 绿色 `el-tag`，调休上班 → 橙色 `el-tag`
- **批量导入弹窗**: 文本域粘贴（支持 `"2024-01-01 元旦 holiday"` 格式），解析后调用 `batchCreateHolidays`
- **与排班联动**: 批量排班时调用 `/holidays/check` 判断是否为节假日，自动跳过放假日期

---

### 2.2 Flutter 移动端

移动端主要是**读取节假日信息**，不需要管理功能。

**`lib/repositories/holiday_repository.dart`** — 新增：

```dart
class HolidayRepository {
  final ApiClient apiClient;
  HolidayRepository({required this.apiClient});

  Future<bool> isHoliday(DateTime date) async {
    final resp = await apiClient.dio.get('/holidays/check', queryParameters: {
      'date': DateFormat('yyyy-MM-dd').format(date),
    });
    if (resp.statusCode == 200 && resp.data['code'] == 200) {
      return resp.data['data']['is_holiday'] == true;
    }
    return false;
  }
}
```

**`lib/screens/calendar/attendance_calendar_screen.dart`** — 日历中标记节假日：

- 加载当月节假日数据
- 放假日期格子背景变浅绿色，显示"休"角标
- 调休上班日期格子显示"班"角标

---

## 三、请假 / 加班审批联动

### 3.1 Web 管理端

审批管理页面已存在（`Notifications.vue`），需要增强。

#### 需要修改的文件

**`src/api/approvals.js`** — 新增/完善 API：

```js
import request from './request'

export const getApprovals = (params) => request.get('/approvals', { params })
export const createApproval = (data) => request.post('/approvals', data)
export const approveApproval = (id, remark) => request.put(`/approvals/${id}/approve`, { remark })
export const rejectApproval = (id, remark) => request.put(`/approvals/${id}/reject`, { remark })
export const deleteApproval = (id) => request.delete(`/approvals/${id}`)
```

**`src/views/Notifications.vue`** — 增强审批页面：

当前页面只有通知列表，需要增加审批 Tab：

```vue
<el-tabs>
  <el-tab-pane label="通知公告" name="notifications">...</el-tab-pane>
  <el-tab-pane label="审批管理" name="approvals">
    <!-- 审批列表 -->
    <el-table :data="approvalList">
      <el-table-column prop="user_name" label="申请人" />
      <el-table-column prop="type" label="类型">
        <template #default="{ row }">
          <el-tag :type="row.type === '请假' ? 'warning' : row.type === '加班' ? 'danger' : 'info'">
            {{ row.type }}
          </el-tag>
        </template>
      </el-table-column>
      <el-table-column prop="reason" label="原因" show-overflow-tooltip />
      <el-table-column prop="status" label="状态">
        <template #default="{ row }">
          <el-tag :type="row.status === 'pending' ? 'warning' : row.status === 'approved' ? 'success' : 'danger'">
            {{ row.status === 'pending' ? '待审批' : row.status === 'approved' ? '已通过' : '已驳回' }}
          </el-tag>
        </template>
      </el-table-column>
      <el-table-column label="操作">
        <template #default="{ row }">
          <el-button v-if="row.status === 'pending'" type="success" size="small" @click="handleApprove(row)">通过</el-button>
          <el-button v-if="row.status === 'pending'" type="danger" size="small" @click="handleReject(row)">驳回</el-button>
        </template>
      </el-table-column>
    </el-table>
  </el-tab-pane>
</el-tabs>
```

- **审批弹窗**: 通过/驳回时弹出 `el-dialog`，输入审批备注
- **联动提示**: 审批通过后显示 Toast："已通过，已自动更新考勤记录"

---

### 3.2 Flutter 移动端

移动端已有审批页面（`approval_list_screen.dart`、`create_approval_screen.dart`），需要增强。

#### 需要修改的文件

**`lib/repositories/approval_repository.dart`** — 已存在，检查是否有 `approve`/`reject` 方法：

```dart
// 确保已有
Future<void> approve(int id, {String? remark}) async {
  await apiClient.dio.put('/approvals/$id/approve', data: {'remark': remark});
}

Future<void> reject(int id, {String? remark}) async {
  await apiClient.dio.put('/approvals/$id/reject', data: {'remark': remark});
}
```

**`lib/screens/approval/approval_list_screen.dart`** — 增强：

- 列表项增加状态标签（待审批/已通过/已驳回）
- 管理员角色显示"通过/驳回"按钮
- 审批通过后显示 SnackBar："审批已通过，考勤记录已更新"

**`lib/screens/home/home_screen.dart`** — 首页快捷入口：

在快捷入口区增加"我的审批"卡片，显示待审批数量角标。

---

## 四、离线打卡

### 4.1 Web 管理端

#### 新增页面

| 页面 | 路由 | 文件 |
|------|------|------|
| 离线打卡记录 | `/offline-checkins` | `views/OfflineCheckins.vue` |

#### 需要修改的文件

**`src/router/index.js`** — 注册路由（放入低频菜单）

**`src/api/offlineCheckins.js`** — 新增 API：

```js
import request from './request'

export const getOfflineCheckins = (params) => request.get('/offline-checkins', { params })
export const syncOfflineCheckins = () => request.post('/offline-checkins/sync')
```

#### `OfflineCheckins.vue` 页面设计

- **顶部**: 筛选（用户、同步状态）+ "一键同步" 按钮
- **表格**: 列：用户、打卡类型、位置、本地时间、同步状态、同步时间
- **同步状态**: 未同步 → 黄色标签，已同步 → 绿色标签
- **一键同步**: 调用 `/offline-checkins/sync`，刷新列表

---

### 4.2 Flutter 移动端

这是移动端的核心功能，需要重点实现。

#### 需要修改的文件

**`lib/repositories/offline_checkin_repository.dart`** — 新增 Repository：

```dart
import 'package:shared_preferences/shared_preferences.dart';
import 'dart:convert';

class OfflineCheckinRepository {
  final ApiClient apiClient;
  static const _cacheKey = 'offline_checkins';

  OfflineCheckinRepository({required this.apiClient});

  /// 缓存离线打卡到本地
  Future<void> cacheOfflineCheckin(Map<String, dynamic> data) async {
    final prefs = await SharedPreferences.getInstance();
    final list = prefs.getStringList(_cacheKey) ?? [];
    list.add(jsonEncode(data));
    await prefs.setStringList(_cacheKey, list);
  }

  /// 获取本地缓存的离线打卡
  Future<List<Map<String, dynamic>>> getCachedCheckins() async {
    final prefs = await SharedPreferences.getInstance();
    final list = prefs.getStringList(_cacheKey) ?? [];
    return list.map((s) => jsonDecode(s) as Map<String, dynamic>).toList();
  }

  /// 同步离线打卡到服务器
  Future<Map<String, dynamic>> syncCheckins() async {
    final resp = await apiClient.dio.post('/offline-checkins/sync');
    if (resp.statusCode == 200 && resp.data['code'] == 200) {
      // 同步成功后清空本地缓存
      final prefs = await SharedPreferences.getInstance();
      await prefs.remove(_cacheKey);
      return resp.data['data'];
    }
    throw Exception('同步失败');
  }

  /// 检查网络状态
  Future<bool> isOnline() async {
    // 可用 connectivity_plus 检查
    return true; // 简化版
  }
}
```

**`lib/blocs/attendance/attendance_bloc.dart`** — 打卡提交逻辑增强：

在 `_onSubmitCheckin` 中，提交失败时（捕获网络异常）缓存到本地：

```dart
Future<void> _onSubmitCheckin(SubmitCheckin event, Emitter<AttendanceState> emit) async {
  // ... 现有逻辑 ...
  try {
    await checkinRepository.submitCheckin(...);
  } catch (e) {
    // 网络异常，缓存离线打卡
    if (e is DioException && e.type == DioExceptionType.connectionError) {
      await offlineRepo.cacheOfflineCheckin({
        'type': event.type,
        'latitude': currentState.currentLatitude,
        'longitude': currentState.currentLongitude,
        'address': currentState.currentAddress,
        'photo': photoUrl,
        'local_timestamp': DateFormat('yyyy-MM-dd HH:mm:ss').format(DateTime.now()),
        'project_id': event.projectId,
      });
      emit(currentState.copyWith(
        checkinFeedback: '网络异常，已缓存离线打卡',
      ));
      return;
    }
    rethrow;
  }
}
```

**`lib/screens/attendance/map_dashboard_screen.dart`** — 增加离线打卡提示：

- 网络断开时显示 Banner："当前离线，打卡将缓存到本地"
- 网络恢复时自动触发同步，显示："已同步 X 条离线打卡"

**`lib/screens/profile/profile_screen.dart`** — 增加"离线打卡记录"入口：

- 点击后进入新页面 `OfflineCheckinsScreen`
- 显示本地缓存的离线打卡列表
- 提供"立即同步"按钮

---

## 五、轨迹回放

### 5.1 Web 管理端

#### 新增页面

| 页面 | 路由 | 文件 |
|------|------|------|
| 轨迹回放 | `/tracks` | `views/Tracks.vue` |

#### 需要修改的文件

**`src/router/index.js`** — 注册路由

**`src/api/tracks.js`** — 新增 API：

```js
import request from './request'

export const getTrack = (userId, date) => request.get(`/tracks/${userId}`, { params: { date } })
export const getHeatmap = (params) => request.get('/tracks/heatmap', { params })
```

#### `Tracks.vue` 页面设计

- **顶部筛选栏**:
  - 人员选择器（`el-select`，从 `/users` 拉取）
  - 日期选择器（`el-date-picker`）
  - "播放轨迹" 按钮
- **左侧信息面板**:
  - 人员信息卡片
  - 统计：总里程、总点数、停留点列表
  - 停留点：开始时间、结束时间、持续时长、地址
- **右侧地图区域**:
  - 使用高德地图 JS API（已有 `AmapSelector.vue` 组件可参考）
  - 绘制轨迹线（蓝色折线）
  - 起点标记（绿色）、终点标记（红色）
  - 打卡点标记（不同图标，点击显示打卡详情）
  - 停留点标记（黄色圆圈）
- **轨迹播放**:
  - 底部播放控制条：播放/暂停、进度条、速度选择（1x/2x/4x）
  - 播放时地图上有一个移动的小车/人图标

---

### 5.2 Flutter 移动端

移动端轨迹回放可以简化，主要是查看自己的历史轨迹。

#### 需要修改的文件

**`lib/repositories/track_repository.dart`** — 新增 Repository：

```dart
class TrackRepository {
  final ApiClient apiClient;
  TrackRepository({required this.apiClient});

  Future<Map<String, dynamic>?> getTrack(int userId, String date) async {
    final resp = await apiClient.dio.get('/tracks/$userId', queryParameters: {'date': date});
    if (resp.statusCode == 200 && resp.data['code'] == 200) {
      return resp.data['data'];
    }
    return null;
  }
}
```

**新增页面 `lib/screens/track/track_screen.dart`**：

- 日期选择器（默认今天）
- 高德地图 WebView 显示轨迹
- 底部卡片显示统计：总里程、停留点数量
- 停留点列表（可点击跳转到地图对应位置）

**`lib/screens/history/history_screen.dart`** — 增加轨迹入口：

- 每条打卡记录右侧增加"查看轨迹"按钮
- 点击后跳转到 `TrackScreen`，传入用户ID和日期

---

## 六、文件改动清单汇总

### Web 管理端（Vue3）

| 动作 | 文件路径 |
|------|----------|
| 新增 | `src/views/Shifts.vue` |
| 新增 | `src/views/Schedules.vue` |
| 新增 | `src/views/Holidays.vue` |
| 新增 | `src/views/OfflineCheckins.vue` |
| 新增 | `src/views/Tracks.vue` |
| 新增 | `src/api/shifts.js` |
| 新增 | `src/api/schedules.js` |
| 新增 | `src/api/holidays.js` |
| 新增 | `src/api/approvals.js`（如不存在） |
| 新增 | `src/api/offlineCheckins.js` |
| 新增 | `src/api/tracks.js` |
| 修改 | `src/router/index.js` — 注册 5 个新路由 |
| 修改 | `src/components/Layout.vue` — 加入导航菜单 |
| 修改 | `src/views/Notifications.vue` — 增加审批管理 Tab |

### Flutter 移动端

| 动作 | 文件路径 |
|------|----------|
| 新增 | `lib/repositories/schedule_repository.dart` |
| 新增 | `lib/repositories/holiday_repository.dart` |
| 新增 | `lib/repositories/offline_checkin_repository.dart` |
| 新增 | `lib/repositories/track_repository.dart` |
| 新增 | `lib/screens/track/track_screen.dart` |
| 新增 | `lib/screens/offline/offline_checkins_screen.dart` |
| 修改 | `lib/blocs/attendance/attendance_bloc.dart` — 获取排班+离线缓存 |
| 修改 | `lib/blocs/attendance/attendance_state.dart` — 新增排班字段 |
| 修改 | `lib/screens/attendance/map_dashboard_screen.dart` — 显示排班+离线提示 |
| 修改 | `lib/screens/calendar/attendance_calendar_screen.dart` — 排班+节假日标记 |
| 修改 | `lib/screens/approval/approval_list_screen.dart` — 审批状态+操作 |
| 修改 | `lib/screens/history/history_screen.dart` — 增加轨迹入口 |
| 修改 | `lib/screens/profile/profile_screen.dart` — 增加离线记录入口 |
| 修改 | `lib/screens/home/home_screen.dart` — 快捷入口增加审批 |
| 修改 | `lib/main.dart` — 注入新 Repository |

---

## 七、实现优先级建议

按以下顺序实现，每完成一个模块就可以联调测试：

1. **班次管理**（Web）→ 排班管理（Web）→ 移动端显示今日排班
2. **节假日管理**（Web）→ 移动端日历标记
3. **审批联动**（Web 审批页面增强 + 移动端审批列表）
4. **离线打卡**（移动端为主，Web 管理端查看同步）
5. **轨迹回放**（Web 管理端为主，移动端简化版）

---

## 八、关键注意事项

1. **日期处理**: 所有日期用本地时间格式化（`YYYY-MM-DD`），不要用 `toISOString()`（会带 UTC 偏移）
2. **坐标转换**: 移动端 GPS 是 WGS84，高德地图用 GCJ02，必须用 `CoordUtils.wgs84ToGcj02` 转换
3. **离线缓存**: 用 `shared_preferences` 存 JSON 字符串，注意容量限制（不要存照片，只存照片 URL 或路径）
4. **权限**: 移动端轨迹回放需要后台定位权限，在 `AndroidManifest.xml` 和 `Info.plist` 中声明
5. **Web 地图**: 轨迹回放用高德地图 JS API 2.0，记得配置安全密钥 `window._AMapSecurityConfig`
